{
  "name": "@nova/confidence-check",
  "version": "1.0.0",
  "description": "Confidence Check plugin: two-phase evaluation (Phase 1 mandatory self-verification + Phase 2 external LLM evaluation with citation/contradiction checking), triggers Socratic revision. Hooks into before_agent_finalize.",
  "type": "module",
  "openclaw": {
    "extensions": [
      "./src/index.ts"
    ],
    "runtimeExtensions": [
      "./dist/index.js"
    ]
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.9.3"
  },
  "devDependencies": {}
}
