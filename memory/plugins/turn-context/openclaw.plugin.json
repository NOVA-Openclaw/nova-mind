{
  "id": "turn-context",
  "name": "Turn Context",
  "description": "Injects per-turn context (reminders, entity identity, semantic recall) into agent prompts",
  "version": "1.0.0",
  "main": "./src/index.ts",
  "hooks": [
    "message_received",
    "before_prompt_build"
  ],
  "activation": {
    "onStartup": true
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "placement": {
        "type": "string",
        "enum": ["system-prepend", "turn-prepend"],
        "description": "Where to inject the dynamic entity/domain/recall block. system-prepend keeps it before the base system prompt (default, no behavior change). turn-prepend moves it adjacent to the current user turn to preserve prompt-cache hits.",
        "default": "system-prepend"
      }
    }
  }
}
