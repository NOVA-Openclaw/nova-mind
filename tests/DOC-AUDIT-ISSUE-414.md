# Documentation Audit — Issue #414

**Date:** 2026-07-11
**Scope:** Technical Writing review of `generate-delegation-context.sh` restore/fix (issue #414), resuming after a prior Technical Writing session died mid-audit with 11 uncommitted doc edits in the working tree.
**Branch:** `feature/issue-414-delegation-context-restore` @ `bcfc26f`

## Method

1. Read both #414 commits in full (`bbc9547` restore/fix, `bcfc26f` QA-blocker fixes) and the resulting `cognition/scripts/generate-delegation-context.sh` source, `agent-install.sh` installer diff, and `tests/install/test_generate_delegation_context.bats` + `tests/TEST-CASES-ISSUE-414.md` to understand exact current behavior (18 test cases, per-section error degradation, env var overrides, installer wiring).
2. Reviewed each of the 11 uncommitted doc edits with `git diff`, verifying every specific claim (SQL query text, column names, file paths, installer behavior, cron schedule, embedding provider/dimensions, trigger existence, script existence) against live sources: the script itself, `agent-install.sh`, `/opt/agents/shared-config/nova-memory-schema.sql`, `memory/agent-install.sh`, `memory/scripts/extract_memories.py`, `relationships/lib/entity-resolver/package.json`, `relationships/nova-ca/`, live cron (`/etc/cron.d/nova-memory-maintenance`), and GitHub issue #271 / nova-cognition repo status.
3. Grepped the full repo for the terms specified in the task brief (`generate-delegation-context.sh`, `delegation-listener`, `DELEGATION_CONTEXT.md`, `workflow_steps_detail`, `agents.seed_context`/`seed_context`, agent-table spawn/seed columns) to find additional docs outside the original 11 that reference the same stale schema.
4. Fixed inaccuracies found; left accurate claims untouched.

## Files Reviewed (11 original uncommitted edits)

| File | Verdict | Notes |
|---|---|---|
| `cognition/CHANGELOG.md` | Accurate, no changes needed | New `#414` entry cross-checked against both commits' diffs, the script source, and the BATS test-case table. All specific claims (dead columns, stderr suppression removal, degradation behavior, apostrophe/heading escaping, installer wiring, `delegation_changed`/#271 gap) verified true. |
| `cognition/docs/delegation-context.md` | Accurate, no changes needed | Rewritten SQL examples match the script's actual queries verbatim. Env var list, output-format description, and "Spawn Instructions" column rebuild all verified against the script and the `agents` table schema (38 columns, no `seed_context`). |
| `cognition/docs/delegation-context-auto-regeneration.md` | Accurate, no changes needed | Verified: `delegation-listener.py` and `delegation-listener.service` do not exist on the host (checked `~/.openclaw/scripts/`, `~/.config/systemd/user/`, `systemctl --user status`). Triggers (`agents_delegation_notify`, `workflows_delegation_notify`, `workflow_steps_delegation_notify`) and the `notify_delegation_change()` function do exist in the live schema and do fire. Issue #271 exists with exactly the content described (fetched via `gh issue view`). `nova-cognition` repo confirmed archived/deprecated in favor of `nova-mind` (fetched via `gh repo view`). |
| `database/schema-reference.md` | **Fixed** | The prior session's "additional drift" note had 3 of its 4 column-count claims backwards. Live schema check: `agents` is 38 cols (doc lists 37 — **real drift, correctly flagged**). But `comms_checks` (11 live, 11 listed), `income_sources` (12 live, 12 listed), and `music_analysis` (11 live, 11 listed) **do not actually differ** — the prior claim that they'd drifted was wrong and has been removed. Also added `motivation_d100` (20 cols) to the "missing from listing" set — it exists live but was omitted from both the doc and the prior audit note. |
| `memory/INSTALLATION.md` | Accurate, no changes needed | Verified `database/pre-migrations/` is populated and read by `agent-install.sh` (`$SCRIPT_DIR/database/pre-migrations`); `memory/pre-migrations/` is empty (`.gitkeep` only). Verified `memory/agent-install.sh` exists but nothing invokes it — it's a stale pre-merge copy. Verified `extract_memories.py` is the actual entry point called by the memory-extract hook (`handler.ts` line 120), not `process-input.sh` (confirmed does not exist anywhere in the repo). |
| `memory/README.md` | **Fixed** | The security-practices bullet claimed `extract_memories.py` "relies on parameterized psql calls" — factually imprecise. The script uses `psycopg2` (Python DB driver), never shells out to the `psql` CLI at all. Corrected to say it "connects to Postgres directly via `psycopg2` (not the `psql` CLI), using parameterized queries." |
| `memory/docs/CONFIDENCE-DECAY.md` | Accurate, no changes needed | Verified against `memory/agent-install.sh`: "Part 4: Scripts Setup" only copies `memory/scripts/*.sh` and `*.py` (confirmed no reference to `memory/templates/` anywhere in the installer), so `memory/templates/memory-maintenance.py` deployment is indeed not installer-managed. Verified "Part 6: Cron Job Setup" installs `/etc/cron.d/nova-memory-maintenance` at `0 6 * * *` running `memory-maintenance.py` — confirmed this cron file exists live with matching content. Dual-trigger (heartbeat + cron) claim is accurate. |
| `memory/docs/agent-delegation-memory.md` | Accurate, no changes needed (pre-verified by prior session) | Confirmed the replaced prompt-text block matches `extract_memories.py` lines 768–769 (`DELEGATION CONTEXT:` block) verbatim. Also independently verified the surrounding status-correction claims: `memory/scripts/embed-delegation-facts.sh` exists; `scripts/seed-delegation-knowledge.sql` does not exist anywhere in the repo but is still referenced (twice) by `memory/scripts/test-delegation-memory.sh` — correctly flagged as a pre-existing code inconsistency, not fixed (out of scope for docs-only work). |
| `memory/docs/semantic-search-guide.md` | **Fixed** | Verified accurate: embedding provider is Ollama/`snowflake-arctic-embed2` at 1024 dims (`memory/scripts/embedding-config.json`), matching live `memory_embeddings.embedding vector(1024)` with no `metadata` column, and zero `embed_*` trigger functions exist in the live schema. **Found and fixed one inaccuracy:** the note claimed embeddings are generated "primarily" by `embed-full-database.py` and `embed-memories.py` "orchestrated via `memory-maintenance.py`," implying three separate coordinating scripts. In fact neither `embed-full-database.py` nor `embed-memories.py` exist as files anymore — `memory-maintenance.py`'s own header states it *replaces* them, and its Phase 2 ("Embed") does the embedding work directly as functions within the single script. Corrected to describe the actual single-script structure. |
| `relationships/CONTRIBUTING.md` | Accurate, no changes needed | Verified no `.env.example` exists anywhere in the repo; `relationships/lib/entity-resolver/package.json` has no `scripts` block at all (so no `npm test`/`npm run lint`); `test.ts` exists and is the actual manual test entry point; no `__tests__/` or `examples/` directories exist; `postgres.json`/`pg-env.ts` claim matches `relationships/ARCHITECTURE-entity-resolver.md`'s own (pre-existing, already-accurate) documentation of the connection-loading order. |
| `relationships/README.md` | Accurate, no changes needed | Verified no `setup-ca.sh` exists anywhere under `relationships/nova-ca/` — only `sign-client-csr.sh` (confirmed its usage signature matches the doc's example command). Verified `nova-ca/certs/ca.crt` already exists in this checkout, and `agent-install.sh` (not a standalone script) handles CA initialization/detection. |

## Additional Docs Found via Grep Sweep (Step 4)

Grepped for `generate-delegation-context.sh`, `delegation-listener`, `DELEGATION_CONTEXT.md`, `workflow_steps_detail`, `agents.seed_context`/`seed_context`, and agent-table spawn/seed columns across the full repo, beyond the original 11 files.

| File | Verdict | Action |
|---|---|---|
| `cognition/README.md` | Accurate | Just links to `delegation-context.md`; no independent factual claims. No change. |
| `cognition/docs/agent-workflow-language.md` | Out of scope, no change | Explicitly a "Status: Design Proposal" DSL spec (v0.1.0). Its `seed_context`/`agent_name` references are illustrative YAML *workflow step output variable names*, not claims about the `agents` table schema. Not a live-system description. |
| `cognition/docs/installation.md` | **Fixed** | Greenfield bootstrap SQL example included `seed_context JSONB` in a starter `CREATE TABLE agents` snippet — the exact dead column #414 fixed. Removed the column and added an explanatory comment pointing to `delegation-context.md` and the real replacement columns, so following this guide from scratch would not reintroduce the bug. |
| `cognition/focus/bootstrap-context/sql/MIGRATION-TEST-RESULTS.md` | **Fixed** | Point-in-time test-results record for issue #53 (2026-02-12), documenting a real migration `agents.seed_context → agents.bootstrap_context`. Verified against the live schema: **neither column exists on `agents` today** — bootstrap context has since moved entirely to the dedicated `agent_bootstrap_context` table (confirmed against `cognition/focus/bootstrap-context/README.md`). Added a "Superseded" note at the top clarifying this is historical only, tying it explicitly to the #414 dead-column confusion (the column name survived across two migrations in stale references even after the underlying storage moved twice). |
| `cognition/focus/templates/context-seed.md` | **Fixed** | Orphaned template doc (not linked from anywhere in the repo) sketching a conceptual `seed_context` JSON shape with no status marker distinguishing design intent from live schema. Added a clarifying note at the top: no such column exists on `agents`; real per-agent bootstrap context lives in `agent_bootstrap_context`, which has a different shape. Left the rest of the conceptual template intact as design material. |
| `cognition/focus/protocols/jobs-system.md`, `memory/docs/integration-overview.md`, `memory/README.md` (existing `agent_jobs` references) | Not applicable, no change | These reference `agent_jobs.agent_name`, a genuinely existing column on a completely different table, unrelated to the `workflow_steps_detail`/`agents.seed_context` dead-column issue. Confirmed `agent_jobs.agent_name varchar(50) NOT NULL` exists in the live schema. |

## Summary of Fixes Applied

1. **`database/schema-reference.md`** — corrected 3 backwards column-count drift claims (comms_checks/income_sources/music_analysis do NOT differ from live; only `agents` does), added missing `motivation_d100` table to the drift note.
2. **`memory/README.md`** — corrected "parameterized psql calls" to accurately describe `psycopg2`-based parameterized queries (no `psql` CLI shell-out).
3. **`memory/docs/semantic-search-guide.md`** — corrected a claim that `embed-full-database.py`/`embed-memories.py` are separate scripts orchestrated by `memory-maintenance.py`; they don't exist anymore, having been absorbed as internal phases of the single script.
4. **`cognition/docs/installation.md`** — removed a `seed_context JSONB` column from a greenfield bootstrap SQL example that would have reintroduced the exact dead-column pattern #414 fixed; added explanatory comment.
5. **`cognition/focus/bootstrap-context/sql/MIGRATION-TEST-RESULTS.md`** — added a "Superseded" note clarifying that neither `agents.seed_context` nor `agents.bootstrap_context` exists today; this historical record predates a second migration that moved bootstrap context to its own table.
6. **`cognition/focus/templates/context-seed.md`** — added a clarifying note that this is design/conceptual material, not a live schema description, given its direct terminology overlap with the #414 dead column.

## Items Out of Scope (Not Fixed, Flagged Only)

- `memory/scripts/memory-catchup.sh` has a real latent bug (`EXTRACT_SCRIPT="${SCRIPT_DIR}/process-input.sh"`, which doesn't exist) — already correctly documented as a known issue in `memory/docs/memory-extraction-pipeline.md` by the prior session. Not a doc problem; needs a Software Engineering fix and its own issue.
- `memory/scripts/test-delegation-memory.sh` references `scripts/seed-delegation-knowledge.sql`, which doesn't exist in the repo — already correctly flagged in `memory/docs/agent-delegation-memory.md`'s status-correction note as a pre-existing code inconsistency, not fixed here (docs-only scope).
- `cognition/docs/agent-workflow-language.md`'s `seed_context` usage is a DSL design-proposal artifact, not a live-schema claim — left untouched.

## Things I Could Not Fully Verify

- Whether `database/schema-reference.md`'s remaining ~93 other table entries (beyond the four explicitly checked: `agents`, `comms_checks`, `income_sources`, `music_analysis`, plus the table-existence diff) have any further column-count drift. A full column-by-column audit of all 97 tables was out of scope for this pass; the existing note already flags this file as due for full regeneration rather than hand-patching, which remains the right call.
- Live confirmation that `cognition/focus/templates/context-seed.md` and `cognition/docs/agent-workflow-language.md` are genuinely never read by any tooling (I confirmed no repo file links to `context-seed.md`, but did not exhaustively search for programmatic/runtime references outside the repo).

## Commit

Documentation-only commit on `feature/issue-414-delegation-context-restore`. No code, test, or fixture files modified. See branch history for commit hash.
